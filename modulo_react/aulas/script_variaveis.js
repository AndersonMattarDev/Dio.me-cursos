const nome = "Pablo"; //Constante, não pode ser alterada
let aprovado = true; //Variável, pode ser alterada

var nota = 8; //Variável, pode ser alterada, mas é recomendável usar let ou const no lugar de var, 
// porque só tem escopo global ou de função, e pode causar problemas de escopo


